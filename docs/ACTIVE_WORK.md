BASE: 931aa156f228d518a7ab39df90b9207a4f4a981b
TASK: Harden and operationalize the machine-level multi-service MCP tunnel manager after architecture red-team review.
BUILD: 0.3.2-rc2
SECURITY CHECKPOINT: 8ee534bbda579e83c09d9f56f2a4c6404e717765 passed Windows security verification.
STATUS: Security hardening and the runtime-distribution boundary are published. RC2 adds exact running-MCP identity: executable SHA-256, staged-bundle verification/source commit, and Tunnel Manager Current/Different/Unverified/Missing preflight.
ARCHITECTURE: Keep the official OpenAI tunnel runtime external and unchanged. McpTunnelRuntime owns neutral supervision/configuration; TaskTrack/PatchTrack domains remain separate. Native channels are routing boundaries, not failure-isolation boundaries.
SECURITY: Session keys are recipient-bound and memory-held; Windows vendor handoff uses a protected named pipe with no plaintext key-file fallback. Child environment is allowlisted; control-plane endpoint pinned and raw HTTP logging disabled. Durable cross-platform encrypted vault remains deferred.
PACKAGING: stage-bin.ps1 creates bin/windows-x64/ only from an exact verify.ps1 verification-manifest. It requires clean source at the verified Git HEAD, unchanged production hashes and the same vendor runtime used by compatibility tests. Bundle contains the four production TaskTrack executables, pinned tunnel-client.exe, verification/bundle manifests, notices/SBOM and README. Vendor artifact is pinned in tunnel-client/runtime-manifest.json.
VALIDATION: Previous Windows security + packaging PASS remains historical at fcd5df8ab6f10cc75fc373dee32b7474e5a27a0f. RC2 changes TaskTrackMcp/Tunnel Manager identity behavior and therefore requires a fresh full verify + stage-bin pass before live acceptance.
OPEN: Manual GUI/live tunnel/ChatGPT multi-channel acceptance; POSIX handoff/process-tree validation; durable vault; vendor stdio child failure still stops whole runtime; strict upstream channel canonicalization/profile persistence audit follow-up; asynchronous health UI deferred.
NEXT: Gary rebuilds/verifies/stages current main as 0.3.2-rc2. Curt then restarts the TaskTrack MCP connection in Codex/host and requires version.build_version=0.3.2-rc2, bundle_verified=true, executable_sha256 and bundle_source_commit matching the new bin manifest before tunnel/live acceptance. Then continue session credential -> Connect -> health/ready -> tunnel_probe/list_dashboards -> activity -> stop/reconnect -> second named channel.
